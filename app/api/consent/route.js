import{NextResponse}from'next/server';import pg from'pg';
const pool=globalThis.__rhc||new pg.Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});globalThis.__rhc=pool;
export async function POST(req){try{const b=await req.json();await pool.query('INSERT INTO consent_events(consent_type,carrier_id,payload) VALUES($1,$2,$3)',['carrier_handoff',b.carrier||null,JSON.stringify({source:'v4_demo',fields:b.fields||[]})]);return NextResponse.json({ok:true},{status:201})}catch(e){return NextResponse.json({error:'Unable to record consent'},{status:500})}}